<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seller Dashboard</title>
    <link rel="stylesheet" href="/style.css">

    <style>
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
            gap: 20px;
            padding: 20px;
        }

        .product-card {
            cursor: pointer;
            text-decoration: none;
            color: #000;
            background: #fff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 6px 15px rgba(0,0,0,0.1);
            transition: all 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-8px) scale(1.03);
            box-shadow: 0 12px 25px rgba(0,0,0,0.2);
        }

        .product-card img {
            width: 100%;
            height: 200px;
            object-fit: scale-down;
            padding: 5px;
        }

        .product-info {
            padding: 12px;
        }

        .price {
            font-weight: bold;
            color: #2e7d32;
        }

        .empty-message {
            padding: 40px;
            text-align: center;
            font-size: 18px;
            color: #666;
        }
    </style>
</head>

<body>

<div class="main-layout">

    <%- include('header') %>

    <main class="content-area">

        <div class="page-header">
            <h1 class="page-title">Dashboard</h1>

            <% if (seller) { %>
                <p>Welcome, <strong><%= seller.name %></strong></p>
            <% } else { %>
                <p>No seller profile found.</p>
            <% } %>
        </div>

        <div class="product-grid">

            <% if (products && products.length > 0) { %>

                <% products.forEach(product => { %>

                    <div class="product-card"
                         onclick="location.href='/sproduct/<%= product.productId %>'">

                        <% if (product.productImages && product.productImages.length > 0) { %>
                            <img src="/uploads/products/<%= product.productImages[0].filename %>"
                                 alt="product image">
                        <% } else { %>
                            <img src="/images/no-image.png" alt="no image">
                        <% } %>

                        <div class="product-info">
                            <h3><%= product.productName %></h3>
                            <p class="price">₹<%= product.price %></p>
                            <p>Stock: <%= product.stock %></p>
                            <p>Status: <%= product.productStatus %></p>
                        </div>

                    </div>

                <% }) %>

            <% } else { %>

                <div class="empty-message">
                   
                </div>

            <% } %>

        </div>

    </main>

</div>

<script>
    <% if (message) { %>
        alert("<%= message %>");
    <% } %>
</script>

</body>
</html>
